# Application Configuration
app.name=Dummy API
app.version=1.0.0
app.environment=development

# Database settings
db.host=localhost
db.port=5432
db.name=dummy

# Logging settings
logging.level=INFO
logging.file.enabled=true
